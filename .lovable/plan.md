
Remove the "Arquivar" option from the task card dropdown menu in `src/features/gestao/components/PmTaskCard.tsx` (line 211). Keep "Renomear" and "Excluir" (admin only). Also remove the now-unused `Archive` icon import and the `handleArchive` function.
